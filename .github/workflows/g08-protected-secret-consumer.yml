name: G08 protected secret consumer
on:
  push:
    branches:
      - main
permissions:
  actions: read
  contents: read
jobs:
  protected-consumer:
    runs-on: ubuntu-latest
    steps:
      - run: echo baseline